## 56.合并区间

### 题目描述

以数组 `intervals` 表示若干个区间的集合，其中单个区间为 `intervals[i] = [starti, endi]` 。请你合并所有重叠的区间，并返回 *一个不重叠的区间数组，该数组需恰好覆盖输入中的所有区间* 。

### 知识点

1. **intervals.sort( (a, b) => a-b )**

   - 遍历数组并对元素两两比较
   - 根据比较函数的结果决定元素顺序
   - 原地修改数组（返回排序后的数组，但原数组也会改变

   ![76493895898](C:\Users\ADMINI~1\AppData\Local\Temp\1764938958989.png)

   ### 答案

```js
var merge = function (intervals) {
  //特殊情况：没有元素或者只有一个元素时 直接返回
  if(intervals === null || intervals.length ===0) return intervals  // 重新从小到大排序
  intervals.sort((a,b) => a[0] - b[0])
  let result = [intervals[0]]   
  for(let i = 1; i < intervals.length; i++) {
    let current = intervals[i]
    let last = result[result.length - 1]    // 当前区间和结果中存放的有重叠 => 修改结果中存放的值
    if(current[0] <= last[1]) last[1] = Math.max(current[1],last[1])
    else result.push(current) 
  }
  return result
};
```

###   思路

result 表示结果数组，初始为第一个区间，current 表示当前的区间，last表示result中最后一个数组

开启遍历，判断 last[1] 和 curtent[0]，合并 => 更新到 result
								不能合并 => 将 current 区间推入 result 数组



比如 intervals = [ [2, 6], [1, 3], [8, 10], [15, 18]]

1.首先要排序 => intervals = [[1, 3], [2, 6], [8, 10], [15, 18]]

2.令result=[ [1,3] ]

3.遍历intervals：intervals[i]中的第一个元素 与 result中最后一个元素中的第一个元素比较 => [2,6] 和 [1,3]比较 => 2小于3 => 要合并 =>令[1,3]中的3为max(6,3) =>result=[ [1,6] ]

## 53.最大子数组和

### 题目描述

给你一个整数数组 nums ，请你找出一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。 子数组是数组中的一个连续部分。

### 答案

```js
var maxSubArray = function (nums) {
  let result = nums[0]
  let temp = nums[0]
  for(let i = 1; i < nums.length; i++) {
    temp = temp + nums[i] > nums[i] ? temp + nums[i] : nums[i]
    result = Math.max(result,temp)
  }
  return result
};
```

思路：动态规划

假设 nums = [-2, 1, -3, 4, -1, 2, 1, -5, 4]

令temp=-2  result=-2

-2 + 1 与 1 比较 => 1大 => 此时 result=1

1 + -3 与 -3 比较 =>  -2大 => 此时 result=1

-2 +4 与 4 比较 => 4大 => 此时result=4

...

## 128.最长连续序列

### 题目描述

给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。

### 答案

```js
var longestConsecutive = function (nums) {
  if(nums.length === 0) return 0
  nums.sort((a, b) => a - b) // 先从小到大排序
  nums.push(1000) // 随便添加一个数
  let temp = nums[0]
  let arr = []
  let result = 1
  // 找到去掉重复数字的数组
  for (let i = 1; i < nums.length; i++) {
    if (nums[i] !== temp) {
      arr.push(temp)
      temp = nums[i]
    }
  }
  // 找到连续数字的个数即为答案
  let sum = 1
  let temp1 = arr[0]
  for (let i = 1; i < arr.length; i++) {
    if (temp1 + 1 === arr[i]) {
      sum++
      temp1++
    }
   else {
      result = Math.max(result, sum)
      temp1 = arr[i]
      sum = 1
    }
  }
  return Math.max(result, sum)
};
```

![76563236496](C:\Users\ADMINI~1\AppData\Local\Temp\1765632364964.png)

## 11.盛最多水的容器

### 题目描述

给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。

找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

### 答案

- 思路：用双指针i，j循环height数，i，j对应高度较小的那个先向内移动，不断计算面积，更新最大面积

```js
var maxArea = function (height) {
    let max = 0 // 返回值
    let square = 0 // 所盛水量 
    let j = height.length - 1 // 右指针
    for (let i = 0; i < height.length ; i++) {
      square = Math.min(height[i], height[j]) * (j - i ) 
      max = Math.max(max,square)
      // 左端比右端大的时候，右指针向左移，左指针应该不变
      if(height[i] > height[j]){
        j--
        i-- //这里-1是为了弥补for循环中的+1 => 左指针不动
      }
    }
    return max
  };
```

## 41.缺失的第一个正数

### 题目

给你一个未排序的整数数组 `nums` ，请你找出其中没有出现的最小的正整数。

请你实现时间复杂度为o(n) 并且只使用常数级别额外空间的解决方案。

### 答案

```js
var firstMissingPositive = function (nums) {
  nums.sort((a,b) => a-b) // 从小到大排列
  if(!nums.includes(1)) return 1 // 没有1
  else {
    const arr = nums.filter(item => item >= 1)   
    for(let i = 0; i < arr.length; i++) {
      if(arr[i] + 1 !== arr[i+1] && arr[i] !== arr[i+1]) return arr[i] + 1
    }
  }   
};
```

先判断数组中有没有1 

没有 => 返回1				

有1 => 找到大于1的数组 遍历，

​	    当后一个数不等于前一个数+1 并且 后一个数不等于前一个数的时候，当前数+1就是缺失的正数

## 238.除自身以外数组的乘积

### 题目

给你一个整数数组 `nums`，返回 数组 `answer` ，其中 `answer[i]` 等于 `nums` 中除 `nums[i]` 之外其余各元素的乘积 。

题目数据 **保证** 数组 `nums`之中任意元素的全部前缀元素和后缀的乘积都在  **32 位** 整数范围内。

请 **不要使用除法，**且在 `O(n)` 时间复杂度内完成此题。

### 答案

```js
 var productExceptSelf = function (nums) {
   const n = nums.length
   const re = []
   const pre = []
   pre[0] = 1
   const sub = []
   sub[n-1] = 1
   // 算左侧除自身外的乘积
   for(let i = 1; i < n; i++) {
     pre[i] = pre[i-1] * nums[i-1] // 左侧当前数 = 左侧前一个数 * 前一个数
   }
   // 算右侧除自身外的乘积
   for(let j = n-2; j >= 0; j--) {
     sub[j] = sub[j+1] * nums[j+1] // 右侧当前数 = 右侧后一个数 * 后一个数
   }

   for(let k = 0; k < n; k++ ){
     re[k] = pre[k] * sub[k] // 对应结果 = 对应左 * 对应右
   }
     
   return re
 };
```

answer[i] 等于 nums 中除了 nums[i] 之外其余各元素的乘积。换句话说，如果知道了 i 左边所有数的乘积，以及 i 右边所有数的乘积，就可以算出 answer[i]。

于是：

定义 pre[i] 表示从 nums[0] 到 nums[i−1] 的乘积。
定义 suf[i] 表示从 nums[i+1] 到 nums[n−1] 的乘积。
我们可以先计算出从 nums[0] 到 nums[i−2] 的乘积 pre[i−1]，再乘上 nums[i−1]，就得到了 pre[i]，即

pre[i]=pre[i−1]⋅nums[i−1]
同理有

suf[i]=suf[i+1]⋅nums[i+1]
初始值：pre[0]=suf[n−1]=1。按照上文的定义，pre[0] 和 suf[n−1] 都是空子数组的元素乘积，我们规定这是 1，因为 1 乘以任何数 x 都等于 x，这样可以方便递推计算 pre[1]，suf[n−2] 等。

算出 pre 数组和 suf 数组后，有

answer[i]=pre[i]⋅suf[i]

## 239.滑动窗口最大值

### 题目

给你一个整数数组 `nums`，有一个大小为 `k` 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 `k` 个数字。滑动窗口每次只向右移动一位。

返回 *滑动窗口中的最大值* 。

```
示例：
输入：nums = [1,3,-1,-3,5,3,6,7], k = 3
输出：[3,3,5,5,6,7]
解释：
滑动窗口的位置                最大值
---------------               -----
[1  3  -1] -3  5  3  6  7       3
 1 [3  -1  -3] 5  3  6  7       3
 1  3 [-1  -3  5] 3  6  7       5
 1  3  -1 [-3  5  3] 6  7       5
 1  3  -1  -3 [5  3  6] 7       6
 1  3  -1  -3  5 [3  6  7]      7
```

答案

![77416928500](C:\Users\ADMINI~1\AppData\Local\Temp\1774169285002.png)

![77416930125](C:\Users\ADMINI~1\AppData\Local\Temp\1774169301259.png)

## 15.三数之和

### 题目

给你一个整数数组 `nums` ，判断是否存在三元组 `[nums[i], nums[j], nums[k]]` 满足 `i != j`、`i != k` 且 `j != k` ，同时还满足 `nums[i] + nums[j] + nums[k] == 0` 。请你返回所有和为 `0` 且不重复的三元组。

**注意：**答案中不可以包含重复的三元组。

**示例 1：**

```
输入：nums = [-1,0,1,2,-1,-4]
输出：[[-1,-1,2],[-1,0,1]]
解释：
nums[0] + nums[1] + nums[2] = (-1) + 0 + 1 = 0 。
nums[1] + nums[2] + nums[4] = 0 + 1 + (-1) = 0 。
nums[0] + nums[3] + nums[4] = (-1) + 2 + (-1) = 0 。
不同的三元组是 [-1,0,1] 和 [-1,-1,2] 。
注意，输出的顺序和三元组的顺序并不重要。
```

**示例 2：**

```
输入：nums = [0,1,1]
输出：[]
解释：唯一可能的三元组和不为 0 。
```

**示例 3：**

```
输入：nums = [0,0,0]
输出：[[0,0,0]]
解释：唯一可能的三元组和为 0 。
```

### 答案

![77418664750](C:\Users\ADMINI~1\AppData\Local\Temp\1774186647504.png)

![77418667376](C:\Users\ADMINI~1\AppData\Local\Temp\1774186673769.png)



## 43.接雨水

### 题目

给定 `n` 个非负整数表示每个宽度为 `1` 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。

**示例 1：**

![77487223151](C:\Users\ADMINI~1\AppData\Local\Temp\1774872231514.png)

```
输入：height = [0,1,0,2,1,0,1,3,2,1,2,1]
输出：6
解释：上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。 
```

### 答案

```js
var trap = function (height) {
  let L = 0; //左指针
  let R = height.length - 1; //右指针
  let L_max = 0; //左边最大值
  let R_max = 0; //右边最大值
  let result = 0;

  while (L <= R) {
    L_max = Math.max(L_max, height[L]);
    R_max = Math.max(R_max, height[R]);
    //左边最大值 < 右边最大值：下标 L 处能接的雨水量等于 L_max - height[L]
    if (L_max < R_max) {
      result = result + (L_max - height[L]);
      L++;
    } else {
      result = result + (R_max - height[R]);
      R--;
    }
  }
  return result;
};
```

**核心思想**：**对于每个位置，能接的雨水量 = min(左边最高柱子, 右边最高柱子) - 当前高度**

- 使用左右指针从两端向中间移动

- 维护左右两侧的**当前最大值**

- 哪边最大值小，就计算哪边的雨水量

  

## 189.轮转数组

### 题目

给定一个整数数组 `nums`，将数组中的元素向右轮转 `k` 个位置，其中 `k` 是非负数。

**示例 1:**

```
输入: nums = [1,2,3,4,5,6,7], k = 3
输出: [5,6,7,1,2,3,4]
解释:
    向右轮转 1 步: [7,1,2,3,4,5,6]
    向右轮转 2 步: [6,7,1,2,3,4,5]
    向右轮转 3 步: [5,6,7,1,2,3,4]
```

### 答案

```js
var rotate = function (nums, k) {
  let n = nums.length;
  let newArray = [];

  for (let i = 0; i < nums.length; i++) {
    newArray[(i + k) % n] = nums[i];
  }

  for (let j = 0; j < nums.length; j++) {
    nums[j] = newArray[j];
  }

  return nums;
};
```

使用额外的数组来将每个元素放至正确的位置。用 n 表示数组的长度，我们遍历原数组，将原数组下标为 i 的元素放至新数组下标为 (i+k)modn 的位置，最后将新数组拷贝至原数组即可。

## 70.爬楼梯

### 题目

假设你正在爬楼梯。需要 `n` 阶你才能到达楼顶。

每次你可以爬 `1` 或 `2` 个台阶。你有多少种不同的方法可以爬到楼顶呢？

**示例 1：**

```
输入：n = 2
输出：2
解释：有两种方法可以爬到楼顶。
1. 1 阶 + 1 阶
2. 2 阶
```

### 答案

```js
var climbStairs = function (n) {
  let p = 0;
  let q = 0;
  let r = 1;
  for (let i = 1; i <= n; i++) {
    p = q;
    q = r;
    r = p + q;
  }
  return r;
};
```

![77487781831](C:\Users\ADMINI~1\AppData\Local\Temp\1774877818317.png)

## 347. 前K个高频元素

### 题目

给你一个整数数组 `nums` 和一个整数 `k` ，请你返回其中出现频率前 `k` 高的元素。你可以按 **任意顺序** 返回答案。

**示例 1：**

**输入：**nums = [1,1,1,2,2,3], k = 2

**输出：**[1,2]

### 答案

![77492331845](C:\Users\ADMINI~1\AppData\Local\Temp\1774923318457.png)



## 152.乘积最大子数组

### 题目

给你一个整数数组 `nums` ，请你找出数组中乘积最大的非空连续 子数组（该子数组中至少包含一个数字），并返回该子数组所对应的乘积。

测试用例的答案是一个 **32-位** 整数。

**请注意**，一个只包含一个元素的数组的乘积是这个元素的值。

**示例 1:**

```
输入: nums = [2,3,-2,4]
输出: 6
解释: 子数组 [2,3] 有最大乘积 6。
```

**示例 2:**

```
输入: nums = [-2,0,-1]
输出: 0
解释: 结果不能为 2, 因为 [-2,-1] 不是子数组。
```

###  答案

```js
var maxProduct = function (nums) {
  if (nums.length === 0) return nums[0];

  const n = nums.length;
  let max = new Array(n);
  let min = new Array(n);

  max[0] = min[0] = nums[0];
  for (let i = 1; i < nums.length; i++) {
    max[i] = Math.max(
      nums[i],
      max[i - 1] * nums[i],
      min[i - 1] * nums[i],
    );
    min[i] = Math.min(
      nums[i],
      max[i - 1] * nums[i],
      min[i - 1] * nums[i],
    );
  }

  return Math.max(...max);
};
```

![77824291486](C:\Users\ADMINI~1\AppData\Local\Temp\1778242914868.png)

## 438.找到字符串中所有字母异位词

### 题目

给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 **异位词** 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

**示例 1:**

```
输入: s = "cbaebabacd", p = "abc"
输出: [0,6]
解释:
起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。
```

 **示例 2:**

```
输入: s = "abab", p = "ab"
输出: [0,1,2]
解释:
起始索引等于 0 的子串是 "ab", 它是 "ab" 的异位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的异位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的异位词。
```

### 答案

```js
function isEqual(a, b) {
  for (let i = 0; i < a.length; i++) {
    if (a[i] !== b[i]) return false;
  }
  return true;
}
var findAnagrams = function (s, p) {
  if (p.length > s.length) return [];

  //长度为26的数组 索引0 => 'a', 索引1 => 'b'
  let pCount = new Array(26).fill(0); // 记录p中每个字母出现的次数
  let winCount = new Array(26).fill(0);

  let result = [];

  // 确定p 初始化第一个窗口
  for (let i = 0; i < p.length; i++) {
    const index = p.charCodeAt(i) - 97;
    pCount[index]++;
    winCount[s.charCodeAt(i) - 97]++;
  }

if (isEqual(pCount, winCount)) result.push(0); //第一个窗口满足条件

for (let j = p.length; j < s.length; j++) {
   const left = s.charCodeAt(j - p.length) - 97;
   winCount[left]--; // 去掉最左边
   const right = s.charCodeAt(j) - 97;
   winCount[right]++; // 添加最右边
   if (isEqual(pCount, winCount)) result.push(j - p.length + 1);
 }
 return result;
};
```

### 思路

1. 用一个固定大小的窗口在 `s` 上滑动，窗口长度 = `len(p)`。
2. 比较窗口内字符的计数是否与 `p` 的字符计数一致。
3. 用数组（或字典）记录 `p` 的字符频率，以及窗口内字符的频率。
4. 每次窗口滑动：
   - 移除左边字符（频率减 1）
   - 加入右边新字符（频率加 1）
   - 比较两个频率数组是否相等
5. 相等时，记录窗口的左边界索引。

![77833506854](E:\Work\qianduan\Algorithms\力扣\asstes\1778335068541.png)

## 76.最小覆盖字串

### 题目

给定两个字符串 `s` 和 `t`，长度分别是 `m` 和 `n`，返回 s 中的 **最短窗口 子串**，使得该子串包含 `t` 中的每一个字符（**包括重复字符**）。如果没有这样的子串，返回空字符串 `""`。

测试用例保证答案唯一。

**示例 1：**

```
输入：s = "ADOBECODEBANC", t = "ABC"
输出："BANC"
解释：最小覆盖子串 "BANC" 包含来自字符串 t 的 'A'、'B' 和 'C'。
```

**示例 2：**

```
输入：s = "a", t = "a"
输出："a"
解释：整个字符串 s 是最小覆盖子串。
```

**示例 3:**

```
输入: s = "a", t = "aa"
输出: ""
解释: t 中两个字符 'a' 均应包含在 s 的子串中，
因此没有符合条件的子字符串，返回空字符串。
```

### 答案

```js
var minWindow = function (s, t) {
   if (t.length > s.length) return ''

   //确定t中每一个字母需要的个数
   let need = new Map()
   for (let char of t) {
       need.set(char, (need.get(char) || 0) + 1)
   }

   let left = 0
   let right = 0
   let required = need.size
   let minStart = 0
   let minLen = Infinity

   let window = new Map()
   //右窗口放大
   while (right < s.length) {
       const c = s[right] //当前右指针指向的字母
       window.set(c, (window.get(c) || 0) + 1)

       if (need.has(c) && window.get(c) === need.get(c)) {
           required--
       }

       while (required === 0) {
           //更新最小字串长度
           if (right - left + 1 < minLen) {
               minLen = right - left + 1
               minStart = left
           }
           //移除s[left]
           const leftchar = s[left]
           window.set(leftchar, window.get(leftchar) - 1)

           if (need.has(leftchar) && window.get(leftchar) < need.get(leftchar)) {
               required++
           }
           //左窗口右移
           left++
       }
       right++
   }
   return minLen === Infinity ? '' : s.substring(minStart, minStart + minLen);
}
```

### 执行过程图解

以 `s = "ADOBECODEBANC", t = "ABC"` 为例：

```
right=0: A [A]D... → need: A-1,B1,C1, required=2
right=1: D [AD].. → required=2
right=2: O [ADO]. → required=2
right=3: B [ADOB] → A1,B0,C1, required=1
right=4: E [ADOBE] → required=1
right=5: C [ADOBEC] → A1,B0,C0, required=0 ✅
收缩 left: [DOBEC] required=0 ✅
收缩 left: [OBEC] required=0 ✅
收缩 left: [BEC] required=1 ❌ 停止收缩
继续扩大...
最终找到 "BANC"
```

## 关键点总结

| 要点       | 说明                                               |
| :--------- | :------------------------------------------------- |
| `need`     | 记录 t 中每个字符还需要多少个                      |
| `required` | 记录还有多少种字符未满足（避免每次都遍历 Map）     |
| 右指针移动 | 扩大窗口，找可行解                                 |
| 左指针移动 | 缩小窗口，找最优解                                 |
| 更新条件   | `window.get(c) === need.get(c)` 时表示该字符已满足 |

## 439.字符串解码

### 题目

给定一个经过编码的字符串，返回它解码后的字符串。

编码规则为: `k[encoded_string]`，表示其中方括号内部的 `encoded_string` 正好重复 `k` 次。注意 `k` 保证为正整数。

你可以认为输入字符串总是有效的；输入字符串中没有额外的空格，且输入的方括号总是符合格式要求的。

此外，你可以认为原始数据不包含数字，所有的数字只表示重复的次数 `k` ，例如不会出现像 `3a` 或 `2[4]` 的输入。

测试用例保证输出的长度不会超过 `105`。

**示例 1：**

```
输入：s = "3[a]2[bc]"
输出："aaabcbc"
```

**示例 2：**

```
输入：s = "3[a2[c]]"
输出："accaccacc"
```

**示例 3：**

```
输入：s = "2[abc]3[cd]ef"
输出："abcabccdcdcdef"
```

### 答案

```JS
var decodeString = function(s) {
    let stack = [] //当前栈
    let str = '' 
    let nums = 0 //数字

    for(let i = 0; i < s.length; i++){
        const char = s[i]

        if(char >= '0' && char <= '9'){
            nums = nums*10 + parseInt(char) //如果是数字，更新数字，注意可能是多位数，所以要乘以10
        }

        //如果是左括号，说明要开始一个新的字符串了，把当前的数字和字符串都压入栈中
        else if(char === '['){
            stack.push(nums)
            stack.push(str)
            nums = 0
            str = ''
        }

        //如果是右括号，说明当前字符串结束了，要把栈顶的字符串和数字弹出来，更新结果字符串
        else if(char === ']'){
            const temp = stack.pop() // 之前的字符串
            const times = stack.pop()
            str = temp + str.repeat(times)
        }
        //[abc] => 记录abc  ( => 遇到'[' => 入栈 => str='' ) 
        else str = str + char
    }
    return str
};
```

### 解题思路

1. 遍历字符串中的每个字符
2. 遇到数字时，记录数字（可能有多位，如 `12[ab]`）
3. 遇到 `[` 时，将当前数字和当前结果压入栈，然后重置
4. 遇到 `]` 时，从栈中弹出前一个数字和结果，将当前结果重复后拼接
5. 遇到字母时，直接拼接到当前结果

```tex
输入: "3[a]2[bc]"
步骤:
1. 遇到 '3' → num = 3
2. 遇到 '[' → 入栈 [3, '']，重置 num=0, str=''
3. 遇到 'a' → str = 'a'
4. 遇到 ']' → 出栈 prevStr='', times=3 → str = '' + 'a'.repeat(3) = 'aaa'
5. 遇到 '2' → num = 2
6. 遇到 '[' → 入栈 [2, 'aaa']，重置 num=0, str=''
7. 遇到 'b' → str = 'b'
8. 遇到 'c' → str = 'bc'
9. 遇到 ']' → 出栈 prevStr='aaa', times=2 → str = 'aaa' + 'bc'.repeat(2) = 'aaabcbc'

输出: "aaabcbc"
```

